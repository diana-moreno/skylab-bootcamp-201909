extends ../view/index
  
block body
    section.view.detail
        h2.detail__title ${title}
        img.detail__image(src="${image}")
        p.detail__description ${description}
        a.detail__store(href="#") ${link}
        span.detail__price ${price}
        span.detail_fav
        form(method="post" action=favPath)
            input(type="hidden" name="id" value="${id}")
            button(type="submit") ${isFav ? '❤️' : '🖤'}
        a.detail__back(href='backPath') Go back