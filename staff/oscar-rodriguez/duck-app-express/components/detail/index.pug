extends ../view/index

block body
    -let { id, title, imageUrl, description, link, price, isFav } = duck
    section.view.detail
        h2.detail__title title
        img.detail__image(src=imageUrl)
        p.detail__description= description
        a.detail__store(href=link) Go to store
        span.detail__price= price
        span.detail__fav
            form(method='post' action=favPath)
                input(type='hidden' name='id' value=id)
                button(type='submit')= isFav ? '🧡' : '💔'
        a.detail__back(href=goBack) Go back